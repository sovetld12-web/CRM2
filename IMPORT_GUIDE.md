# 📥 Инструкция по загрузке данных из старой CRM

## ✅ Что сделано

Загрузчик данных полностью переработан и теперь поддерживает:

1. ✅ **Полный бэкап из старой CRM** (формат `agency-crm-json`)
2. ✅ **Автоматическую конвертацию** полей из старого формата в новый
3. ✅ **Отдельные файлы** для каждого типа данных
4. ✅ **Подробный лог импорта** с информацией о каждом шаге
5. ✅ **Поддержку разных форматов** JSON

---

## 🎯 Как загрузить данные

### Шаг 1: Откройте вкладку "Загрузка данных"

В боковом меню найдите **"Загрузка данных"** (вторая вкладка, зелёная иконка ☁️⬆️)

### Шаг 2: Загрузите JSON файл

**Вариант A: Drag-and-drop**
- Перетащите JSON файл в большую зону загрузки

**Вариант B: Кнопка**
- Нажмите "Выбрать файл JSON"
- Выберите файл из старой CRM

### Шаг 3: Дождитесь импорта

Система автоматически:
1. Определит формат данных
2. Конвертирует из старого формата в новый
3. Сохранит все данные в localStorage
4. Перезагрузит страницу через 3 секунды

### Шаг 4: Проверьте данные

После перезагрузки проверьте:
- ✅ Лиды (вкладка "Лиды")
- ✅ Проекты (вкладка "Производство")
- ✅ Задачи (вкладка "Задачи")
- ✅ Финансы (вкладка "Деньги")
- ✅ Спящая база (вкладка "Спящая база")

---

## 📋 Поддерживаемые форматы

### ✅ Формат 1: Полный бэкап из старой CRM (agency-crm-json)

**Теперь полностью поддерживается!** Система автоматически распознаёт и конвертирует все данные.

```json
{
  "app": "CRM Команда роста",
  "format": "agency-crm-json",
  "version": 2,
  "exportedAt": "2026-09-08T14:50:27.197Z",
  "counts": {
    "leads": 116,
    "sleepingLeads": 101,
    "orders": 38,
    "bankTransactions": 341,
    "tasks": 15
  },
  "data": {
    "leads": [...],
    "sleepingLeads": [...],
    "orders": [...],
    "bankTransactions": [...],
    "tasks": [...]
  }
}
```

**Что импортируется автоматически:**
- ✅ Лиды (`data.leads`)
- ✅ Спящая база (`data.sleepingLeads`)
- ✅ Проекты/Заказы (`data.orders`)
- ✅ Финансовые операции (`data.bankTransactions`)
- ✅ Задачи (`data.tasks`)

**Автоматическая конвертация полей:**

| Старое поле | Новое поле |
|-------------|------------|
| `amount` | `sum` |
| `nextDate` | `nextStepDate` |
| `owner` | `responsible` |
| `note` | `comment` |
| `contactHandle` | `phone` |
| `lastTouchDate` | `lastContactDate` |
| `leadCreatedAt` | `createdAt` |
| `company` | `client` (для проектов) |

**Лог импорта в консоли:**
```
📥 Начинаем импорт файла: crm-komanda-rosta-2026-09-08.json
📊 Структура данных: ['app', 'format', 'version', 'data']
✅ Обнаружен формат: полный бэкап CRM
📦 Доступные данные: ['leads', 'sleepingLeads', 'orders', 'bankTransactions', 'tasks']
📥 Импортируем лидов: 116
📥 Импортируем спящую базу: 101
📥 Импортируем проектов: 38
📥 Импортируем финансовых операций: 341
✅ Импорт завершен. Успешно: 596, Ошибок: 0
```

### Формат 2: Отдельные файлы

**leads.json** — массив лидов:
```json
[
  {
    "id": "lead_1",
    "date": "2026-01-15",
    "contact": "Иван Иванов",
    "company": "ООО Пример",
    "source": "Профи.ру",
    "stage": "Заявка",
    "product": "Рекрутинг",
    "sum": 50000
  }
]
```

**projects.json** — массив проектов:
```json
[
  {
    "id": "proj_1",
    "client": "ООО Пример",
    "vacancy": "Менеджер по продажам",
    "sum": 80000,
    "status": "В работе"
  }
]
```

**money.json** — массив финансовых операций:
```json
[
  {
    "id": "tx_1",
    "date": "2026-01-15",
    "type": "income",
    "sum": 50000,
    "counterparty": "ООО Клиент"
  }
]
```

**tasks.json** — массив задач:
```json
[
  {
    "id": "task_1",
    "title": "Позвонить клиенту",
    "priority": "high",
    "dueDate": "2026-01-20"
  }
]
```

---

## 🔍 Если загрузка не работает

### Проверьте консоль браузера

1. Откройте консоль (F12)
2. Перейдите на вкладку "Console"
3. Загрузите файл
4. Посмотрите лог импорта:

```
📥 Начинаем импорт файла: backup.json
📊 Структура данных: ['app', 'format', 'version', 'data']
✅ Обнаружен формат: полный бэкап CRM
✅ Конвертировано лидов: 116
✅ Конвертировано спящей базы: 101
✅ Конвертировано заказов: 38
✅ Конвертировано транзакций: 341
✅ Конвертировано задач: 15
📦 Импортируем: ['leads', 'sleepingClients', 'projects', 'moneyOperations', 'tasks']
✅ Сохранено лидов: 116
✅ Сохранено спящей базы: 101
✅ Сохранено проектов: 38
✅ Сохранено финансовых операций: 341
✅ Сохранено задач: 15
```

### Проверьте формат JSON

Убедитесь, что файл:
- ✅ Имеет расширение `.json`
- ✅ Содержит валидный JSON
- ✅ Не поврежден

**Проверка валидности JSON:**
- Откройте файл в текстовом редакторе
- Скопируйте содержимое
- Вставьте на [jsonlint.com](https://jsonlint.com)
- Нажмите "Validate JSON"

### Попробуйте загрузить по отдельности

Если полный бэкап не загружается:

1. Извлеките лиды из `data.leads` → сохраните в `leads.json`
2. Извлеките проекты из `data.orders` → сохраните в `projects.json`
3. Загрузите каждый файл отдельно

---

## 📊 Что импортируется

### Лиды (data.leads)

**Поля:**
- `id` → `id`
- `date` → `date`
- `company` → `company`
- `contact` → `contact`
- `contactHandle` → `phone`
- `source` → `source`
- `stage` → `stage`
- `product` → `product`
- `project` → `project`
- `amount` → `sum`
- `paid` → `paid`
- `nextStep` → `nextStep`
- `nextDate` → `nextStepDate`
- `owner` → `responsible`
- `comment` → `comment`
- `leadCreatedAt` → `createdAt`

### Спящая база (data.sleepingLeads)

**Поля:**
- `id` → `id`
- `company` → `client`
- `contact` → `contact`
- `contactHandle` → `phone`
- `source` → `source`
- `product` → `product`
- `project` → `project`
- `amount` → `ltv`
- `nextStep` → `nextStep`
- `nextDate` → `nextStepDate`
- `lastTouchDate` → `lastContactDate`
- `comment` → `comment`

### Проекты (data.orders)

**Поля:**
- `id` → `id`
- `company` → `client`
- `vacancy` → `vacancy`
- `amount` → `sum`
- `status` → `status`
- `startDate` → `startDate`
- `contact` → `contact`
- `phone` → `phone`
- `paid` → `paid`
- `comment` → `comment`

### Финансовые операции (data.bankTransactions)

**Поля:**
- `id` → `id`
- `date` → `date`
- `type` → `type`
- `counterparty` → `counterparty`
- `category` → `category`
- `paymentType` → `paymentType`
- `sum` → `sum`
- `description` → `description`

### Задачи (data.tasks)

**Поля:**
- `id` → `id`
- `title` → `title`
- `priority` → `priority`
- `source` → `source`
- `dueDate` → `dueDate`
- `done` → `done`
- `createdAt` → `createdAt`

---

## 💡 Советы

### Перед загрузкой

1. **Сделайте резервную копию** текущих данных:
   - Откройте "Загрузка данных"
   - Нажмите "Скачать JSON"
   - Сохраните файл

2. **Проверьте формат файла**:
   - Убедитесь, что это валидный JSON
   - Проверьте структуру данных

3. **Очистите старые данные** (если нужно):
   - Откройте консоль браузера (F12)
   - Выполните: `localStorage.clear()`
   - Перезагрузите страницу

### После загрузки

1. **Проверьте все вкладки**:
   - Лиды
   - Производство
   - Задачи
   - Деньги
   - Спящая база

2. **Проверьте графики на дашборде**:
   - Воронка продаж
   - Источники лидов
   - История показателей

3. **Сделайте экспорт данных**:
   - Откройте "Загрузка данных"
   - Нажмите "Скачать JSON"
   - Сохраните файл для резервного копирования

---

## 🆘 Проблемы и решения

### Проблема: "Не удалось определить тип данных"

**Решение:**
- Проверьте структуру JSON файла
- Убедитесь, что есть поле `data` с массивами
- Попробуйте загрузить файл через консоль:

```javascript
const file = /* ваш файл */;
const reader = new FileReader();
reader.onload = (e) => {
  const data = JSON.parse(e.target.result);
  console.log('Структура:', Object.keys(data));
  console.log('Данные:', Object.keys(data.data || {}));
};
reader.readAsText(file);
```

### Проблема: "Данные импортированы, но не отображаются"

**Решение:**
- Перезагрузите страницу (Ctrl+F5)
- Очистите кэш браузера
- Проверьте localStorage:

```javascript
console.log('Лидов:', JSON.parse(localStorage.getItem('crm_leads') || '[]').length);
console.log('Проектов:', JSON.parse(localStorage.getItem('crm_projects') || '[]').length);
```

### Проблема: "Некоторые поля пустые"

**Решение:**
- Проверьте лог импорта в консоли
- Убедитесь, что поля есть в исходном файле
- Проверьте маппинг полей (таблица выше)

---

## 📞 Поддержка

Если проблема не решена:

1. Откройте консоль браузера (F12)
2. Скопируйте лог импорта
3. Проверьте структуру JSON файла
4. Попробуйте загрузить файл через консоль
5. Обратитесь в поддержку с логами

---

**Версия инструкции:** 1.0  
**Дата обновления:** 2026-09-08
